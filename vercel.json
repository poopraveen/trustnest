{
  "buildCommand": "DATABASE_URL=${DATABASE_URL:-postgresql://build:build@localhost/build} prisma generate && ([ -n \"$DATABASE_URL\" ] && prisma db push --accept-data-loss || echo 'Skipping db push: DATABASE_URL not set') && next build",
  "framework": "nextjs"
}
